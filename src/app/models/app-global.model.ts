import { Injectable } from '@angular/core';
import { UserDetail, IUserDetail } from '.';
// import { LocalStorageService } from 'angular-2-local-storage';

@Injectable()
export class AppGlobal {

    // userDetails: UserDetail = new UserDetail(this._storage.get<IUserDetail>('userdetails'));
    constructor() {

    }
}

